-- ============================================================================
-- TeamFirst Migration 013 — user_role 에 'marketer' 추가
-- Date: 2026-06-26
--
-- ⚠️ 반드시 014보다 먼저, 그리고 014와 "별도 실행"하세요.
-- Postgres는 새 enum 값을 추가한 트랜잭션 안에서 그 값을 사용할 수 없습니다.
-- (013 단독 실행 → 커밋 → 014 실행)
-- ============================================================================

ALTER TYPE public.user_role ADD VALUE IF NOT EXISTS 'marketer';

-- ============================================================================
-- 017: user_role enum에 'payback' 추가 (페이백 플랫폼 광고주 롤)
-- Date: 2026-08-12
--
-- ⚠️ PG enum 제약: 이 파일은 반드시 단독 실행하고, 018 실행 전에 커밋되어야 함.
--    (같은 트랜잭션에서 새 enum 값 사용 불가 — D-055/013 전례와 동일)
-- ============================================================================

ALTER TYPE user_role ADD VALUE IF NOT EXISTS 'payback';

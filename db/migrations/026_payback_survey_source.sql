-- ============================================================================
-- TeamFirst Migration 026 — 이탈 설문 입력 위치 구분
-- Date: 2026-08-28
--
-- 설문이 어디서 응답됐는지 기록:
--   'apply'   = 정식 신청 페이지(/apply) 진입 후 노출된 설문
--   'landing' = 랜딩 간편 신청 팝업(약식)을 제출 없이 닫아 노출된 설문
-- ============================================================================

ALTER TABLE public.pb_apply_surveys
  ADD COLUMN IF NOT EXISTS source TEXT;

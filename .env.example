# ============================================================================
# TeamFirst - Environment Variables
# ============================================================================
# 이 파일을 .env.local로 복사 후 실제 값을 채우세요.
# .env.local은 절대 커밋하지 마세요. (.gitignore에 이미 등록되어 있음)
# ============================================================================

# ----------------------------------------------------------------------------
# Supabase
# ----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# 서버 전용 (절대 클라이언트에 노출 금지)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ----------------------------------------------------------------------------
# 앱 URL
# ----------------------------------------------------------------------------
# 개발: http://localhost:3000
# 프로덕션: https://app.teamfirst.kr
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ----------------------------------------------------------------------------
# Resend (이메일 발송)
# ----------------------------------------------------------------------------
RESEND_API_KEY=re_xxxxxxxxxxxx
RESEND_FROM_EMAIL=noreply@teamfirst.kr
RESEND_REPLY_TO=hello@teamfirst.kr

# 운영자 필수 알림 수신 주소 (매칭요청·광고주 가입·대행사 등록신청 접수 시 발송)
# 콤마로 여러 명 지정 가능. 미설정 시 users.role='admin' 계정 이메일로 발송.
ADMIN_NOTIFY_EMAIL=team1st2025@gmail.com

# ----------------------------------------------------------------------------
# OAuth (선택 - 카카오 로그인 추가 시)
# ----------------------------------------------------------------------------
# 카카오는 Supabase Auth Provider 설정에서 별도로 등록
# 여기에는 클라이언트 ID만 필요한 경우에 한해 추가

# ----------------------------------------------------------------------------
# n8n Webhook (선택 - 자동화 연동)
# ----------------------------------------------------------------------------
N8N_WEBHOOK_URL=https://n8n.your-host.com/webhook/xxxx
N8N_WEBHOOK_SECRET=your-shared-secret

# ----------------------------------------------------------------------------
# 글로싸인 (Phase 2 자동화 시)
# ----------------------------------------------------------------------------
# GLOSIGN_API_KEY=xxx
# GLOSIGN_API_URL=https://api.glosign.com

# ----------------------------------------------------------------------------
# Sentry (선택 - 에러 트래킹)
# ----------------------------------------------------------------------------
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# ----------------------------------------------------------------------------
# 운영자 시드 계정 (초기 셋업 시 사용)
# ----------------------------------------------------------------------------
SEED_ADMIN_EMAIL=admin@teamfirst.kr
SEED_ADMIN_PASSWORD=change-me-immediately
